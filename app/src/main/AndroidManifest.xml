<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dong.covid19app">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--    인터넷 권한 허용-->

    <application
        android:name="com.dong.covid19app.config.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_cohak"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_cohak_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name="com.dong.covid19app.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.dong.covid19app.src.MainActivity"></activity>
    </application>

</manifest>